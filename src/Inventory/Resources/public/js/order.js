"use strict";

$(document).ready(function () {
	$('#sizeIterator').val(window.submittedForm.sizeIterator);
	$('#orderName').val(window.submittedForm.orderName);
	$('#creativeName').val(window.submittedForm.creativeName);
	$('#creativeTemplateId').val(window.submittedForm.creativeTemplateId);
	$('#advertiserId').val(window.submittedForm.advertiserId);
	$('#traffickerId').val(window.submittedForm.traffickerId);
});
